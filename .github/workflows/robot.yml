name: mi primer robot 

on:
  push:
    branches: [delete]

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - name: checkout 
        uses: actions/checkout@v4
      - name: saludo
        run: echo " ${{ secrets.DOCKERHUB_TOKEN }} "